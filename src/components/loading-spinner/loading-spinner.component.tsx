import {
    Spinner,
    SpinnerContainer
} from './loading-spinner.style';

const LoadingSpinner = () => {

    return (
        <SpinnerContainer>
            <Spinner />
        </SpinnerContainer>
    )

}

export default LoadingSpinner

